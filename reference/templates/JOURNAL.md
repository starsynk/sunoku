<!-- sunoku:stub -->
# Journal — <product>

> Append-only. Entry types: track | reshape | decision. Newest at the bottom.
> Entry header pattern (machine-scanned): `## YYYY-MM-DD — <type>`
> Optional per-entry lines — **Tags:** comma, separated (powers `report.mjs --tag`) and
> **By:** author (attribution on team repos).
