# Contract — delivery-planner (gap-plan hat)

The output contract in `delivery-planner-full-plan.md` applies in full, with one narrowing: the
plan covers only the dispatch's named gap list — every task row's Trace points at a gap-list item
(or the as-built evidence that scopes it), and no task re-plans work the as-built PRD already
shows working. Milestone 1 is still the thinnest end-to-end slice of the gaps.
